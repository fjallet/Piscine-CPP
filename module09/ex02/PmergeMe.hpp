#ifndef RPN_HPP
# define RPN_HPP

#include <list>
#include <vector>
#include <algorithm>
#include <iostream>
#include <ctime>
#include <sstream>

std::list<int>	mergeList(std::list<int> & lst);

std::vector<int>	mergeVector(std::vector<int> & lst);

#endif
